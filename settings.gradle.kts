// When building locally: these add conflicting module names, so pick one at a time

// includeBuild("ideal-case")
includeBuild("workaround-case-1")

rootProject.name = "gradle-included-build-settings-repositories"
